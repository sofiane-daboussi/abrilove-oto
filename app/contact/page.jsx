import ContactClient from './ContactClient'

export const metadata = {
  title: 'Contact — Abrilove',
  description: 'Une question ? Un message ? Écris-nous, on te répond.',
}

export default function ContactRoute() {
  return <ContactClient />
}
